class OddNumbers{
    static main(_string_){
       for (int  = 0; number < 100; number++){
           if (number % 2 != 0){
               system.out.printin(number);
           };
       };
   };
};

